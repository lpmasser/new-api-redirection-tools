services:
  app:
    build: .
    container_name: new-api-redirection-tools
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      # 数据持久化 - SQLite 数据库
      - ./data:/app/server/data
    env_file:
      - .env
